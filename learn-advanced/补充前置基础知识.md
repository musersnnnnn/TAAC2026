Embedding 本质上就是“查表”。
假设：
nn.Embedding(num_embeddings=100, embedding_dim=64)
模型会创建一个大小为：
[100, 64]
的参数矩阵。每个离散 ID 对应其中一行：
ID = 3
↓
取第 3 行
↓
得到 64 维向量
例如：
user_int_feats_1 = 3
就会取出 ID 为 3 对应的 Embedding 向量。